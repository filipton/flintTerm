[package]
name = "term-ghostty"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
term-core.workspace = true
ghostty-vt-sys.workspace = true
log.workspace = true

[dev-dependencies]
# Only for the `vtbench` example, which times the two backends against each other.
term-alacritty.workspace = true
